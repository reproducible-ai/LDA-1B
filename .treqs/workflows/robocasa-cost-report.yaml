name: LDA-1B temporary-compute cost measurement
secrets:
  - HF_TOKEN
measure:
  command: |
    set -euo pipefail
    timeout --signal=TERM --kill-after=30 7200 bash .treqs/scripts/run_cost_calibration.sh
  trace: "off"
